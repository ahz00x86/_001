<Page
    x:Class="BigIntegersCalculator.TextoCrypto"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:BigIntegersCalculator"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="Silver">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        
        <!-- TíTULO -->
        <TextBlock Grid.Row="0" Grid.ColumnSpan="2" Text="Criptografía / Cryptography" HorizontalAlignment="Center" Margin="5"/>

        <StackPanel Grid.Row="1" Grid.ColumnSpan="2" Orientation="Horizontal">
            <TextBlock Text="Contraseña / Password:" Margin="5"/>
            <TextBox x:Name="txtPassword" Text="Contraseña de prueba / Test Password" Margin="5" />
        </StackPanel>

        <!-- ENCRIPTAR -->
        <TextBlock Grid.Row="2" Grid.Column="0" Text="Texto / Text:" Margin="5"/>
        <TextBox Grid.Row="3" Grid.Column="0" x:Name="txtEncriptar" Height="auto" Width="auto" TextWrapping="Wrap" AcceptsReturn="True" Margin="5"/>
        <Button Grid.Row="4" Grid.Column="0" x:Name="btnEncriptar" Click="btnEncriptar_Click" Content="Encriptar / Encrypt" Margin="5" HorizontalAlignment="Center"/>        
        
        <!-- DESENCRIPTAR -->
        <TextBlock Grid.Row="2" Grid.Column="1" Text="Texto Encriptado / Encrypted Text:" Margin="5"/>
        <TextBox Grid.Row="3" Grid.Column="1" x:Name="txtDesencriptar" Height="auto" Width="auto" TextWrapping="Wrap" AcceptsReturn="True" Margin="5"/>
        <Button Grid.Row="4" Grid.Column="1" x:Name="btnDesencriptar" Click="btnDesencriptar_Click" Content="Desencriptar / Decrypt" Margin="5" HorizontalAlignment="Center"/>        

    </Grid>
</Page>
