<Page
    x:Class="BigIntegersCalculator.Grafica"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:BigIntegersCalculator"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    SizeChanged="Page_SizeChanged"
    Loaded="Page_Loaded"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid x:Name="gridP">
        <StackPanel Orientation="Vertical">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                <Button Content="- Zoom" Click="menosZoom_Click" Margin="5"/>
                <TextBlock Text="GRÁFICA / GRAPH" Margin="5"/>
                <Button Content="+ Zoom" Click="masZoom_Click" Margin="5"/>
            </StackPanel>
            <TextBlock x:Name="Formula" Text="Error" Margin="5" HorizontalAlignment="Center"/>
            <!--<Button x:Name="InvokePrintingButton" Content="Print" Click="OnPrintButtonClick"/>-->
            <Canvas x:Name="canvasGrafica" Background="White">
                <Polyline x:Name="lineaPL" Stroke="Red"
                    StrokeThickness="2"                          
                    Points="{Binding}" Canvas.ZIndex="1" />
            </Canvas>
        </StackPanel>
    </Grid>
</Page>
